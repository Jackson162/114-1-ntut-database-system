<!DOCTYPE html>
<html>
<head>
    <title>Checkout</title>
</head>
<body>
    <h1>填寫收件資訊</h1>

    {% if error %}
        <div class="error" style="color: red; border: 1px solid red; padding: 10px; margin-bottom: 10px;">
            <h3>結帳失敗</h3>
            <p>原因：{{ error }}</p>
        </div>
    {% endif %}

    <form method="post" action="/frontend/customers/orders/create">
        
        <label for="recipient_name">收件人姓名:</label><br>
        <input type="text" id="recipient_name" name="recipient_name" value="{{ recipient_name or '' }}" required><br><br>

        <label for="recipient_phone">電話:</label><br>
        <input type="text" id="recipient_phone" name="recipient_phone" value="{{ recipient_phone or '' }}" required><br><br>

        <label for="recipient_address">地址:</label><br>
        <input type="text" id="recipient_address" name="recipient_address" value="{{ recipient_address or '' }}" required><br><br>

        <input type="submit" value="確認下單">
    </form>
    
    <br>
    <a href="/frontend/customers/cart">返回購物車</a>
</body>
</html>