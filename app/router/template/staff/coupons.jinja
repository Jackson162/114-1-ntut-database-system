<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Book Coupons</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>

<body>
<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>My Coupons</h1>
        <a href="/frontend/staffs/coupons/create" class="btn btn-primary">Create New Coupon</a>
    </div>

    {% if coupons %}
    <div class="list-group">
        {% for coupon in coupons %}
        <div class="list-group-item list-group-item-action border-0 shadow-sm rounded mb-3">
            <div class="d-flex w-100 align-items-center">
                <!-- Delete Button on the left -->
                <form action="/staffs/coupons/{{ coupon.coupon_id }}/delete" method="post" class="me-3">
                    <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this coupon?');">
                        <i class="bi bi-trash"></i> Delete
                    </button>
                </form>

                <!-- Coupon Details -->
                <div class="flex-grow-1">
                    <div class="d-flex w-100 justify-content-between">
                        <h5 class="mb-1">{{ coupon.name }}</h5>
                        <small class="text-muted">
                            {{ coupon.start_date }} to {{ coupon.end_date if coupon.end_date else 'N/A' }}
                        </small>
                    </div>
                    <p class="mb-1">
                        <strong>Type:</strong> <span class="badge bg-secondary">{{ coupon.type }}</span>
                    </p>
                    <p class="mb-1">
                        <strong>Discount:</strong> {{ (coupon.discount_percentage * 100) | round(2) }}%
                    </p>
                    <small class="text-muted">Coupon ID: {{ coupon.coupon_id }}</small>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
    {% else %}
    <div class="alert alert-info" role="alert">
        No coupons for this bookstore.
    </div>
    {% endif %}
</div>
</body>